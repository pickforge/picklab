{
  "name": "@pickforge/picklab-core",
  "version": "0.1.0",
  "private": true,
  "description": "Config, sessions, artifacts, manifests, and process supervision for PickLab",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  }
}
